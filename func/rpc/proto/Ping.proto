syntax = "proto3";

option optimize_for = LITE_RUNTIME;

package rpc;

message PingRequest {
    string message = 1;
}

message PingResponse {
    string message = 1;
}

service PingSvc {
    rpc Ping(PingRequest) returns (PingResponse);
}